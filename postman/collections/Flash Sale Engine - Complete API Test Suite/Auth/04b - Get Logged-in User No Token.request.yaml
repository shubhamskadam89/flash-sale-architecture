$kind: http-request
name: 04b - Get Logged-in User No Token
url: "{{baseUrl}}/api/v1/auth/me"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Get Logged-in User No Token: Expects 401 Unauthorized', function () {
          pm.expect(pm.response.code).to.equal(401);
      });
    language: text/javascript
order: 4100
