// TODO_BACKEND_MAPPING:
// Add the confirmed SSE endpoint path and event payload contract here.
// The app intentionally does not create EventSource subscriptions until that contract exists.
export type InventorySsePayload = Record<string, unknown>;
